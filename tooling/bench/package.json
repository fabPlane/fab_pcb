{
  "name": "@fp-pcb/bench",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Latency benchmarks for the KiCad API transports (direct ws, ipc, bridge)",
  "scripts": {
    "latency": "bun run transport-latency.ts"
  },
  "dependencies": {
    "@fp-pcb/bridge": "workspace:*",
    "@fp-pcb/client": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  }
}
